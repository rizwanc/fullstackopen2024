import ReactDOM from 'react-dom/client'

import App from './App'


// Renders the App.jsx contents into a div element, defined in index.html, having the id value
// root.
ReactDOM.createRoot(document.getElementById('root')).render(<App />
)
